name: Apex Wealth Lab Autopilot Engine

on:
  schedule:
    - cron: '0 6,13,20 * * *'
  workflow_dispatch:

jobs:
  build-and-run:
    runs-on: ubuntu-latest

    steps:
    # 1. 🎯 This forces a deep, clean clone of your files straight into the workspace root
    - name: Checkout Repository Code
      uses: actions/checkout@v4
      with:
        clean: true

    - name: Set up Python Environment
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install FFmpeg Multimedia Engine
      run: |
        sudo apt-get update
        sudo apt-get install -y ffmpeg

    - name: Install Project Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install google-generativeai edge-tts requests google-api-python-client google-auth-oauthlib google-auth-httplib2

    # 2. 🔍 This version WILL run the list command so we can see what's happening if it fails
    - name: Run Automation Engine
      env:
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        YOUTUBE_CLIENT_ID: ${{ secrets.YOUTUBE_CLIENT_ID }}
        YOUTUBE_CLIENT_SECRET: ${{ secrets.YOUTUBE_CLIENT_SECRET }}
        YOUTUBE_REFRESH_TOKEN: ${{ secrets.YOUTUBE_REFRESH_TOKEN }}
      run: |
        echo "=== Checking Workspace Path ==="
        pwd
        echo "=== Listing Root Files ==="
        ls -la
        echo "=========================="
        python main.py
